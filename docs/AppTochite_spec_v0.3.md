# AppTochite — Функциональная спецификация v0.3

**Версия:** 0.3 · Апрель 2026  
**Платформа:** PWA · Android (90%) · интерфейс на русском языке

---

## 1. Обзор продукта

AppTochite — мобильное приложение для профессиональных заточников ножей. Решает задачу ведения журнала заточек: фиксация клиентов, ножей, камней, параметров заточки и рекомендаций.

**Целевая аудитория:** два сегмента с единым интерфейсом:

- **Заточник как малый бизнес** — клиенты, учёт заработка, статусы
- **Заточник «для себя»** — личный журнал, коллекция

**Ключевое решение:** оба сегмента используют единый интерфейс. Пользователь «для себя» представлен как **нулевой клиент («Я»)** — создаётся автоматически при первом запуске, не удаляется.

---

## 2. Навигация

Нижняя панель навигации (bottom navigation bar) — постоянная, доступна со всех экранов.

**Вкладки:**

- **Клиенты** — стартовый экран
- **История** — единая лента всех заточек
- **Справочники** — три под-вкладки: Камни / Стали / Ножи
- **+ Заточка** — кнопка создания (не вкладка), доступна с любого экрана

> 📌 Кнопка «+ Заточка» при нажатии из карточки клиента (C-2) предзаполняет поле клиента в форме и скрывает его.

---

## 3. Реестр экранов

### 3.1 Клиенты

#### C-1 — Список клиентов

- Поиск по имени или телефону
- Нулевой клиент «Я» закреплён отдельной секцией сверху — не удаляется
- Строка клиента: аватар с инициалами, имя, телефон, счётчик заточек
- Кнопка «Добавить клиента» — пунктирная строка внизу списка
- Подзаголовок аппбара: общее число клиентов и заточек

> 📌 Удаление клиента — только через карточку C-2, без свайпа.

#### C-2 — Карточка клиента

- Шапка: аватар с инициалами, имя, телефон, Телеграм
  - Тап по телефону → копирует в буфер, показывает toast «Телефон скопирован»
  - Тап по Телеграму → открывает `t.me/username` в браузере (редирект в приложение)
- Метрики: заточек всего, суммарная выручка
- Кнопки: «Редактировать», «+ Новая заточка»
- История заточек клиента: нож, сталь + угол, цена, статус-пилюля

#### C-3 — Добавить / редактировать клиента

- «Готово» в аппбаре — неактивен пока имя пустое
- Кнопка «Добавить / Сохранить» дублирует действие внизу формы
- «Отмена» — возврат без сохранения

| Поле | Описание | Обяз. |
|---|---|:---:|
| Имя | текстовое поле | ✱ |
| Телефон | маска `+7 (___) ___-__-__` | |
| Телеграм | префикс `@`, username без собаки | |

- Режим редактирования: внизу ссылка «Удалить клиента» (красная, без рамки) → открывает M-1

> 📌 Удаление клиента удаляет все его заточки.

---

### 3.2 История

#### H-1 — Лента заточек

- Аппбар: «История» + подзаголовок «N заточек · X ₽»
- Поиск по клиенту, марке ножа, стали, камням — подсвечивает совпадение в строке
- Фильтр чипами по статусу: Все / Принят / В работе / Готов
- Три метрики: заточек, выручка, в работе
- Лента сгруппирована по месяцам
- Строка: нож + дата, клиент, сталь + угол, цена, статус-пилюля
- Тап на строку → Z-2
- Внизу ленты — секция «Данные» с двумя действиями: экспорт и импорт

#### Экспорт данных

- Тап «Экспортировать данные» → bottom sheet с предупреждением о размере файла
- Кнопка «Экспортировать» → стандартный Android share sheet (Телеграм, Google Drive, загрузки и т.д.)
- Формат файла: `apptochite_backup_ГГГГ-ММ-ДД.json`
- Содержимое: все клиенты, заточки, пользовательские справочники, фото в base64

> 📌 Файл может занять несколько десятков МБ из-за фото в base64.

#### Импорт данных

- Тап «Импортировать данные» → нативный файловый picker Android
- Если данные уже есть → bottom sheet с предупреждением: «Импорт полностью заменит текущие данные. Это действие нельзя отменить»
- Кнопки: «Заменить и восстановить» (красная) / «Отмена»
- Прогресс-бар с подписью «Пожалуйста, не закрывайте приложение»
- Toast по завершении: «Данные восстановлены · N заточек, N клиентов»

---

### 3.3 Создание и просмотр заточки

#### Z-1 — Форма заточки (stepper: Приёмка → Заточка)

**Шаг 1 — Приёмка:**

| Поле | Описание | Обяз. |
|---|---|:---:|
| Клиент | bottom sheet с поиском. Скрыто если пришли из C-2 | ✱ |
| Марка ножа | автодополнение из справочника + кнопка добавления новой | ✱ |
| Сталь | автодополнение из справочника + кнопка добавления новой | |
| HRC | выбор из диапазона 50–70, одно значение или пусто | |
| Тип ножа | кухонный / охотничий / складной / топор / другой | |
| Состояние при приёме | мультивыбор чипами: просто тупой / замятие / выкрошка / скол / ржавчина | |
| Дата приёма | по умолчанию сегодня | ✱ |

> 📌 После сохранения шага 1 предлагается фото «До» (можно пропустить).

**Шаг 2 — Заточка:**

| Поле | Описание |
|---|---|
| Угол заточки | числовое поле, градусы на сторону |
| Камни | тап → M-2 (bottom sheet с поиском по справочнику). Теги с порядком. Drag-to-reorder. |
| Комментарий и рекомендации | textarea, 2–3 строки, растягивается |
| Цена | числовое поле, ₽ |
| Статус | принят / в работе / готов, по умолчанию «принят» |

#### Z-2 — Детальная запись (просмотр)

- «Изменить» — только в аппбаре справа
- Шапка: клиент (аватар + имя), марка ножа, чипы (тип, сталь, HRC)
- Статус-пилюля + цена + даты (принят / готов)
- Состояние при приёме — чипами
- Заточка: угол, камни тегами с порядком
- Комментарий
- Фото: два слота «до» и «после»
  - Баннер «Добавьте фото до заточки» — появляется после сохранения шага 1
  - Баннер «Добавьте фото после заточки» — появляется при смене статуса на «готов»
  - Каждый баннер можно пропустить
- «Удалить заточку» — тихая красная ссылка внизу страницы, без рамки → M-1

> 📌 Дата готовности проставляется автоматически при статусе «готов».

---

### 3.4 Справочники

#### S-1 / S-2 / S-3 — Камни / Стали / Ножи

- Переключение сегментированным контролом (3 вкладки)
- Поиск в каждой вкладке
- Список сгруппирован по категориям
  - Камни: водные / алмазные / масляные
  - Стали: японские / европейские / американские / китайские / российские / порошковые
  - Ножи: японские кухонные / немецкие / скандинавские / американские / российские / китайские
- Внизу каждой вкладки: «Добавить [позицию] в справочник» → M-3

#### S-4 — Карточка позиции

- Один шаблон для всех трёх справочников, поля адаптируются
- Read-only
- Кнопка «Использовать в заточке» → создание заточки с предзаполненным полем

**Поля камня:** бренд, зернистость, тип (водный / масляный / алмазный), описание

**Поля стали:** название, HRC, рекомендуемый угол, описание

**Поля ножа:** бренд, страна, сталь, рекомендуемый угол, тип, описание

---

## 4. Модальные оверлеи

#### M-1 — Подтверждение удаления

- Появляется при удалении клиента (из C-3) или заточки (из Z-2)
- Текст предупреждения о последствиях
- Кнопки: «Отмена» и «Удалить» (красная)

#### M-2 — Выбор камней

- Bottom sheet с поиском по справочнику
- Мультивыбор галочками
- Кнопка подтверждения: «Добавить N камней»
- Результат: теги в поле формы с порядковым номером
- Удаление тега — крестик, изменение порядка — drag-to-reorder удержанием

#### M-3 — Добавить позицию в справочник

- Bottom sheet с полями новой позиции (зависят от вкладки)
- Сохраняется локально

#### M-4 — Фото кромки

- Открывает камеру устройства
- Превью снятого фото
- Кнопки: «Переснять» и «Сохранить»

---

## 5. Сквозные UX-решения

### Toast-уведомления

- «Телефон скопирован» — при тапе на телефон в C-2
- Всплывает снизу, исчезает через 2–3 секунды, не требует действия

### Статусы заточки

| Статус | Цвет | По умолчанию |
|---|---|---|
| принят | синий | при создании |
| в работе | янтарный | вручную |
| готов | зелёный | вручную → автоматически ставит дату готовности |

### Фото до / после

- Фото «До» — предлагается после сохранения шага 1 (Приёмка)
- Фото «После» — предлагается при смене статуса на «готов»
- Оба фото можно пропустить
- До 2–3 фото на заточку, съёмка камерой устройства

### Автодополнение

- Поля «Марка ножа» и «Сталь» — подсказки при вводе из справочника
- Кнопка добавления новой позиции → M-3
- Пользовательские позиции сохраняются локально

---

## 6. Предзаполненные справочники

**Камни (~28 позиций):** DMT, King, Naniwa, Chosera, Suehiro, Shapton, Arkansas, Атома

**Стали (~100+ позиций):** японские, американские, европейские, российские, китайские, порошковые

**Ножи (~200+ позиций):** японские кухонные, немецкие, скандинавские, американские складные, российские, китайские

---

## 7. Бэклог (за рамками MVP)

**Высокий приоритет:**
- 🔁 Быстрое создание заточки — «повторить как прошлый раз»

**Средний приоритет:**
- 🔁 Быстрое создание заточки — «повторить как прошлый раз»
- 📱 Шаринг результата — карточка заточки как PDF или картинка для клиента
- 🤖 Распознавание ножа по фото — Claude API
- 📊 Статистика — заточек за период, выручка, топ камней, топ сталей
- 🔔 Уведомление клиенту — «нож готов», интеграция с SMS или WhatsApp
- 🌐 Общие облачные справочники — пополняются сообществом

**Низкий приоритет:**
- ☁️ Облачная синхронизация — несколько устройств
- 👥 Мультипользовательский режим
- 🌍 Мультиязычность

---

## Changelog

**v0.3 (апрель 2026) — UX-сессия:**

- Поле «Финиш» удалено полностью из формы заточки и спецификации
- Добавлено поле «Телеграм» в карточку клиента (`@username`, открывает `t.me/username`)
- Телефон в C-2: тап копирует в буфер + toast, не открывает звонок
- Форма заточки Z-1 разбита на stepper: **Приёмка → Заточка**
- Фото «До» привязано к событию приёмки (после шага 1), «После» — к статусу «готов»
- Поле «Клиент» скрыто в форме если пришли из карточки клиента C-2
- Выбор клиента в форме — bottom sheet с поиском
- Удаление клиента и заточки — только через карточку с подтверждением, без свайпа
- «Изменить» в Z-2 — только в аппбаре, без дублирующей кнопки внизу
- «Удалить заточку» — тихая ссылка без рамки внизу Z-2
- Навигация: 3 вкладки + кнопка «+ Заточка»
- Бэклог: экспорт и импорт перенесены в MVP — секция «Данные» в разделе История

**v0.2 — первичная спецификация.**
